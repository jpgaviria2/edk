document.querySelectorAll('[data-menu-toggle]').forEach((button)=>{button.addEventListener('click',()=>{const menu=document.querySelector('#mobile-menu');if(!menu)return;const open=menu.classList.toggle('open');button.setAttribute('aria-expanded',String(open));});});
document.querySelectorAll('[data-quiz]').forEach((quiz)=>{const feedback=quiz.querySelector('.feedback');quiz.querySelectorAll('.choice').forEach((button)=>{button.addEventListener('click',()=>{quiz.querySelectorAll('.choice').forEach((b)=>b.classList.remove('correct','wrong'));const ok=button.dataset.answer==='correct';button.classList.add(ok?'correct':'wrong');if(feedback)feedback.textContent=ok?'Correct — keep going.':'Not quite. Try the answer that explains the situation best.';});});});
